# Supabase Configuration
# Used for database and storage
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Clerk Configuration
# Authentication and user management
# IMPORTANT: Use LIVE keys (pk_live_...) for plugsy.ng and TEST keys (pk_test_...) for development
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
VITE_CLERK_FRONTEND_API=your_clerk_frontend_api # Fallback if custom domain DNS fails
CLERK_SECRET_KEY=your_clerk_secret_key
# Required by every authenticated Vercel API route, including browser push
# registration and message delivery. Copy these from Clerk's JWT/session-token
# configuration; use the exact production domain(s), comma-separated.
CLERK_JWT_ISSUER=https://your-clerk-issuer
CLERK_AUTHORIZED_PARTIES=https://www.plugsy.ng,https://plugsy.ng
# Optional: set when your Clerk token uses an audience claim.
CLERK_JWT_AUDIENCE=

# AI Integration
# Never use a VITE_ prefix for AI-provider secrets: Vite embeds those values in
# the browser bundle. Keep this server-only and rotate any key that was ever
# configured as VITE_GEMINI_API_KEY.
GEMINI_API_KEY=
# OpenAI for additional chatbot capabilities
OPENAI_API_KEY=

# Communications & Payments
# Resend for transactional emails
RESEND_API_KEY=
# Flutterwave for creator monetization and wallet payouts
VITE_FLUTTERWAVE_PUBLIC_KEY=
FLUTTERWAVE_SECRET_KEY=
FLUTTERWAVE_SECRET_HASH=
# Cron secret for scheduled tasks
CRON_SECRET=

# Assets & Media
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REFRESH_TOKEN=

# App Environment
NEXT_PUBLIC_SITE_URL=https://www.plugsy.ng
# AI Studio automatically injects APP_URL at runtime

# Notifications (server-only; never prefix provider secrets with VITE_)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Push Notifications (OneSignal)
VITE_ONESIGNAL_APP_ID=
ONESIGNAL_APP_ID=
ONESIGNAL_APP_API_KEY=
# Deprecated migration fallback; remove after production verification.
ONESIGNAL_REST_API_KEY=

# Video Calling
DAILY_API_KEY=
